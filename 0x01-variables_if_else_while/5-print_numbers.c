#include <stdio.h>
/**
* main -Entry point
* description: print digit number of base 10
* Return: always 0
*/
int main(void)
{
	int b;

	for (b = 0; b <= 9; b++)
	{
		printf("%d", b);
	}
	printf("\n");
return (0);
}
