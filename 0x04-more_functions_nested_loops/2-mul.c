#include "main.h"
#include <stdio.h>
/**
* mul - multiply two numbers
* @x: first number
* @y: second number
* Return: always success(0)
*/

int mul(int x, int y)
{
	int result = x * y;

	return (result);
}
